# Handoff — 2026-09-21, branch `analysis-rework`

Written for whoever picks this up next, including a future session with none of
this one's context. It supersedes `HANDOFF_2026-09-20.md` on state; that document
remains the right place for the lift, incident F5 and the lessons of the run's
first day, and `HANDOFF_2026-09-13.md` for the reasoning behind the 2026-09-13
remediations. **Read `docs/EXECUTION_PLAN.md` after this** (the Status block at
the top, then the session log). Load `context-capsule load` first if the shims
exist; otherwise read `docs/CONTEXT_CAPSULE.md` and `docs/LOCAL_SETUP.md`.

---

## 0. Orientation in one paragraph

The question is whether public awareness of police violence changes what New
Yorkers ask EMS for. Outcome: mental-health / EDP call share and count at
community-district × day level, 59 districts. Treatment: a composite national
attention index built from Wikipedia pageviews on a basket of police-violence
articles plus US Google Trends. Design: stacked episode event study with
episode-level randomization inference under a confirmation freeze — 2017–2020
explorable ("discovery"), 2015-07→2016-12 and 2021→2024 sealed ("confirmation";
strata C1 clean of B-HEARD and C2 exposed to it). The freeze was lifted at
`4e3dce1` on 2026-09-20 after every stratum's null was certified at 1,000
simulations; one sealed script (`30_confirmatory_run.py`) ran once and one
mechanical reader (`34_confirmatory_reading.py`) read it once. **The confirmatory
result is the pre-specified null in both strata** (PRE_ANALYSIS_NOTE 9.4 row 5,
read by addendum §19 rule 2). The pre-registration is `CONFIRMATION_PLAN.md`
(frozen text + addendum §1–§30) and `PRE_ANALYSIS_NOTE.md`; the discovery result is
a bounded null too. The manuscript (`docs/PAPER.md`) is complete and has passed a
full adversarial audit (CP3); the one open item is the author's editorial pass.

## 1. State at handoff (22:50Z; the 05:30Z rows are revised in place)

| | |
|---|---|
| Branch | `analysis-rework`, pushed through `d999d84` (the referee-panel revision) plus the record commit after it; working tree clean. The Obsidian vault is outside the repository (`vault/` gitignored since `6ace8f5`; delivered as a zip) |
| Phase I | **Sealed 2026-09-21 03:24:13Z**: `data/reference/confirmatory_results.csv` (166 cells, 2,000 draws, 157 estimated, 9 `IDENTICAL_TO_PRIMARY`) with `.meta.json` (source/input hashes, commit b046fb6, hash seed 0); `confirmatory_run_log.csv` with seven start rows (the hand relaunches carry their reasons, the two automatic retries repeat the preceding one, the first start carries none) and the sealed row; `confirmatory_reading.csv` (175 rows) with its sidecar. Committed `34d4b9e` |
| Reading | Both strata "does not reject"; sustained shift ≥ pre-freeze MDE of the EDP share (0.01156 C1 / 0.00875 C2; about 0.0148 / 0.0112 under the approximate family correction) disfavoured at 80% power; MEI −0.005 not excluded; transient dip-and-rebound of the MEI disfavoured (0.00382 / 0.00290); placebos quiet. Reported, not read: C1 smallest unadjusted p 0.0115, 14 of 24 H1-outcome sensitivity cells at p ≤ 0.05, dose-response positive (asymptotic p 0.03 / 0.02). C2: 0 of 30; no secondary movement. Pooled: does not reject on both arms |
| Manuscript | `docs/PAPER.md` restructured to the venue's shape (`03e3d8e`: prose Results, four display items, `docs/SUPPLEMENT.md` S1–S6) and revised on a five-referee panel (`d999d84`: intervals beside the verbatim pre-registered reading, the C1 path as sealed, the guards outcome by outcome, the count arm as a rate on the same denominator, the identifying assumption, plain language; `docs/REFEREE_PANEL_2026-09-21.md` carries every comment, its fact-check and its disposition). Abstract 250 words; main text 4,998 words (ceiling 5,000; the venue's ~4,000 not reached); four display items |
| CP3 | **Done.** Blind half 2026-09-20 (wf_f6cc235c); full audit 2026-09-21 (wf_0df7b965-8dc: 8 finders, 61 raw, 57 unique, 10 refuted by three lenses, 5 survived, critic 6). Every surviving item fixed and every unverified item that held on inspection fixed; filed as P59–P63, held by `S.third_pass_record_consistency` tokens (defeat-tested). Scripts: scratchpad `cp3-manuscript-audit.js`, `cp3_fixes.py`, `cp3_tables.py`, `cp3_figures.py` |
| Claims | 1,582 registered, 1,582 verify (292 hand-written + 1,290 generated); every numeric table cell of PAPER_MASTER, PAPER and SUPPLEMENT covered (the regenerated episode table is held by re-rendering instead) |
| Gate | 92 checks, 92/92 twice after every edit (`docs/regression_baseline.csv` at 92); new since 05:30Z: `V.paper_budget` (W1), `V.manuscript_referee_tokens` (RP1–RP6) |
| Findings | 171 filed, 171 fixed, 0 open; incidents F1–F5 disclosed |
| References | 11 (Kang, Lu & Pang; Psychiatr Serv 2026;77(7):616–622), 12 (IBO, January 2026, with URL), 13 (Hölzl, Keusch & Sajons; Soc Sci Res 2025;126:103099) confirmed against their sources |
| Open for the author | The seven items in EXECUTION_PLAN's Status block (22:50Z): the venue's ~4,000 words; whether to keep the within-period-family disclosure and the intervals-beside-the-bound framing; the raw outcome series as a main-text figure; a tracked generator for a heterogeneity Figure S7; a counted linkage flow for RECORD 6.3; ethics, affiliations, funding, conflicts; RECORD 6.2 |

## 2. What this session did (2026-09-20 10:50Z → 2026-09-21 05:30Z)

1. **Babysat the sealed run to the seal.** Two out-of-memory kills of the worker
   pool (13:48Z with four workers; 20:17Z with three, on the pooled 60-day cells),
   each followed by the runner's automatic retry (stopped by hand, its start row
   kept) and a hand relaunch with fewer workers and an accurate reason. Every
   start is a run-log row; parallelism changes no number (per-cell identity
   seeds and ledgers). Monitor cycles kept the session active.
2. **Ran the reader once**, sealed to the table; committed both with sidecars.
3. **Generated the tables, the prose and the claims** (`ops/phase_i_tables.py`)
   and pasted them: PAPER_MASTER §8b and PAPER.md's confirmatory Results.
4. **Wrote the Discussion's reading and implications paragraphs, the Methods'
   seal paragraph and the Abstract from the reading's sentences only**; removed
   the PHASE I PENDING markers; cut the Abstract to the plan's specification.
5. **Confirmed references 11–13** against PubMed, the Mannheim repository and
   the IBO report; RECORD 6.2 states what Kang, Lu & Pang analysed.
6. **Ran the full CP3 audit and remediated it** (§4 lists what it found). The
   fixes were prepared while the refuters ran, dry-run on copies, and applied
   after the workflow returned so the refuters judged the text the finders saw.
7. Updated vault, graph, capsule, execution plan and this handoff at each step.

## 2b. What the later sessions did (05:30Z → 22:50Z)

1. **Reorganised the Obsidian vault** from the graph, then removed it from the repository at the user's
   request (`6ace8f5`, `d56852e`; `vault/` gitignored; rendered locally by `python3 -m graph_memory.obsidian
   --vault <path>` and delivered as a zip).
2. **Assessed the paper** for the user (the C1 pattern is a shape, not a level; the raw-count dip; the weak
   bound; the verbatim-reader style; the length; the missing supplement; the non-blind items; the venue).
3. **Restructured it** (`03e3d8e`): `docs/SUPPLEMENT.md`; prose Results; four display items; the first-week path
   figures and the decade figure; `V.paper_budget`.
4. **Ran a five-referee panel** (wf_a3619d51-50e) with a fact-check per major/moderate comment, prepared the
   fixes while it ran, applied them after it returned, and held the corrections with `V.manuscript_referee_tokens`
   (`d999d84`). The panel's record and every comment's disposition: `docs/REFEREE_PANEL_2026-09-21.md`.
5. Updated the execution plan, this handoff, the capsule, the graph export and the local vault at each step.

## 3. Decisions (user, standing) and how they were honoured

- Continue to Phase I as pre-registered; a non-rejection is read against the
  pre-freeze MDE (addendum §19) — the reading is code, the power table is
  tracked and flagged pre-freeze, and the paper quotes the reader verbatim.
- Work on `analysis-rework`, commits authored Abrahim Mahmud, no AI attribution,
  named paths only, push `origin analysis-rework` — every commit this session.
- O2 disclosed then run as a declared access — done earlier (§18; F-log).
- Deliverable: Markdown manuscript `docs/PAPER.md` with the repo at CP3 — done.
- Standing don'ts honoured: the legacy lag-7 result is never restated; no
  specification, lag, sample or outcome definition changed after the lift; the
  C1 sensitivity pattern and the dose-response sign are reported as hypotheses,
  not findings, and nothing was re-run to chase them; no graph-memory hooks
  installed; 00/00b never run outside the cold passes.

## 4. What I got wrong, so you do not repeat it

- **The paper's account of the record was softer than the record** (CP3's five
  surviving findings): F1/F2 called "metadata reads" and F3 a "coverage read"
  when all three read outcome values; "never used for any specification
  choice" (F2's EDPM decision, F4's weights); "no number from them was kept"
  (the F4 weights are in the B-HEARD exposure covariate); "before any
  confirmation-period outcome was seen" (F1–F3 precede the commitments); and a
  closing claim that the compositional damping biases toward the null when it
  runs in the predicted direction. Lesson: when the paper paraphrases the
  record, quote the record's own nouns; the suite now holds the corrected
  wording (P59–P63).
- **A generator omission hid nine sealed cells**: Table 8b.3 looped over C1 and
  C2 only, so the pooled falsification and diagnostic rows reached no exhibit.
  Every table that says "per stratum" must loop over every stratum in the file.
- **A Methods definition drifted from the code**: "divided by all dispatches"
  omitted the disposition filter; the episode label was described as one
  registry death when Table 1 carries the 60-day candidates; the freeze
  architecture was described pre-D8. Read the generator and the docstring
  before writing the sentence, not after.
- **A table label that begins with a numeral is a value to the exhibit check**
  ("28-day post window"), and a mechanical rename of it spliced into prose
  ("post window 60 dayss") in three places. Rename by whole-cell match only.
- **A citation marker inserted into a claim's anchor sentence breaks the claim**
  (C161). Grep the register for the sentence before adding a reference.
- **A Monitor filter that embeds python in a single-quoted shell string prints
  nothing** (the escaped quotes become a syntax error on stderr); the watch
  expired with "no events" while the workflow was working. Keep monitor scripts
  to grep and cut.
- **A workflow prompt written before the run finished went stale** ("resumed
  once"); re-read every factual statement in a prompt before launching it.
- Carried from 2026-09-20 (still binding): the container suspends minutes after
  the session goes idle; check runs by PID; stamp sidecars after every edit to
  a hashed source; never grep the script's own name from a command whose text
  contains it (match `[s]pawn_main`); always run the defeat attempt.

## 5. What to do next, in order

1. Nothing is in flight. The repository is at `d999d84` plus the record commit, gate 92/92.
2. **Author decisions on the manuscript** (EXECUTION_PLAN Status, 22:50Z): whether to cut toward the venue's
   ~4,000 words (candidates: Limitations 7–9 and 11–12 to the supplement; the within-period-family sentence; the
   guards sentence) — every number moves through its claim and `V.paper_budget` must stay green; whether the
   within-period family-of-four disclosure and the intervals beside the pre-registered bound stay (both are
   additions to what the pre-registration reads out; the reading is the conclusion either way); whether the raw
   outcome series replaces Table 2 as a display item.
3. **Supplement additions that need artifacts first**: a tracked generator for the discovery heterogeneity
   figures before a Figure S7 can be cited; registered counts for a RECORD 6.3 linkage flow (records removed by
   the disposition filter, dispatches without a district, district-days under the five-dispatch rule).
4. **RECORD 6.2**: read Kang, Lu & Pang's methods and write the code-list comparison.
5. **Front matter at submission**: the ethics determination (stated as pending in Methods), affiliations,
   funding and conflict-of-interest lines, the journal's word count.
6. Reconcile the Mac-side vault and graph by import (`graph-memory import docs/graph/graph_memory.jsonl`; unzip
   the vault delivered with this session over `~/Downloads/abrahimm/EMS`).

## 6. Conventions that are not obvious

- Every long job runs through a runner in `ops/` with a PID file and a state
  file under `ops/state/` (gitignored); poll by PID, never `pgrep -f`; a second
  real start of 30 needs `PHASE_I_RESUME_REASON`, which is recorded.
- Every change passes the gate twice; every new check or token gets a defeat
  attempt; the baseline is refreshed in the commit that adds a check.
- Register CSVs are CRLF and are written through the csv module (templates may
  contain a wrapped newline inside the quoted field; that is the convention).
- Table labels never begin with a numeral; a claim's anchor must be re-checked
  whenever the sentence it sits in is edited, including a reference marker.
- Generated claims are replaced wholesale when the generator changes (rows whose
  note names `ops/phase_i_tables.py`), never edited by hand.
- `PYTHONHASHSEED=0` and single-threaded BLAS for anything whose bytes matter;
  the runners export them; the gate is run the same way.
- The capsule, the graph export and `vault/` are updated with every commit that
  changes state; the Mac copies are reconciled by import.
