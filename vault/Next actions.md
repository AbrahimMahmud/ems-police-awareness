# Next actions

1. Cold passes match → commit `outputs/run_manifest.json` and the registers the cold run rewrites.
2. Discovery and C2 certificates at 1,000 sims → `S.lift_requires_1000_sims` satisfied → gate clean → baseline.
3. The lift: `config.FREEZE_ACTIVE = False` in one greppable commit; `ops/phase_i.sh` (30 at 2,000 draws, then 34).
4. `ops/phase_i_tables.py` → PAPER_MASTER §8b and PAPER.md Results/Discussion/Abstract with claims; figures into
   `docs/figures/`; CP3 audit; EXECUTION_PLAN session log; reconcile this vault and graph with the Mac copies.

Open: the Registered Report route is foreclosed by Phase I (supervisor's call); the specification audit's
refutation phase has not completed on any run (usage limits).

See [[Status]] · [[Decisions]]
