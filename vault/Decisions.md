---
tags: [ledger, decisions]
updated: 2026-09-21
---
# Decisions

Mirror of `docs/CONTEXT_CAPSULE.md` (the ledger) and the `decision` nodes of the graph ([[Graph memory]]).
Reasons live there; this note is the dated list.

## 2026-09-13
- Continue to Phase I as pre-registered; a non-rejection is read against the pre-freeze MDE (addendum §19).
- Branch `analysis-rework`; commits authored Abrahim Mahmud, no AI attribution; deliverable `docs/PAPER.md`.
- O2 coverage diagnostic disclosed (addendum §18) then run as a declared access.
- Addendum §25 fallback for an uncertified null, written before the 1,000-sim verdict; not invoked.
- The reading is code (34); placebo override = cardiac and asthma; 28/60-day windows and the dose arm in the seal.
- Exploratory scripts pin the discovery window; the lift opens one door (addendum §26).

## 2026-09-20
- Discovery p-values at 2,000 draws; gate clean; baseline refreshed.
- Repo-tracked memory tooling built here; Mac copies reconciled by import.
- Every pipeline stage runs under `PYTHONHASHSEED=0` and one BLAS thread (X20) so cold passes match to the byte.
- Third CP2 specification audit remediated in full before the lift (P27–P58, addendum §30; rules 23.1c sensitivity
  set, 23.1d pooled reading, 23.1e secondary arms). Sealed run: run log, hash seed, design gate, 14-day draw
  geometry, unfiltered diagnostics, cross-window coverage-clean + geocoding-only, cancelled-inclusive and no-EDPM
  on both arms, path columns, pinned sidecars. Reader: sealed, pre-freeze power table, placebo check cannot fail open.
- Record corrected where it contradicted the code (row 3 non-blind; C1 clean of B-HEARD with its 2021 block inside
  COVID; §6 strata; row 30 B-HEARD covariate; §18/F4/§20 wording; 62,920 → 77,506; three docstrings; §5.3's remedy
  foreclosed by the lift — the author's 2026-09-19 decision).
- Discovery certified at 1,000 sims; the 1.28 family-correction factor kept (conservative) with the 8-df 1.21 beside it.
- The lift: `FREEZE_ACTIVE = False` in its own commit, after discovery, C1 and C2 certified at 1,000 sims and the
  gate ran clean in the lifted state. Incident F5 found by that gate run, disclosed before the commit
  ([[Freeze incidents]]).

## 2026-09-21
- Phase I's seven starts are recorded, not hidden: the hand relaunches carry their reasons in the run log, the two
  automatic retries repeat the preceding reason, the sealed table's `overwrite_reason` carries the last;
  parallelism (4 → 2 → 3 → 2 workers) changes no number. Methods and §8b state the history ([[Phase I]]).
- The confirmatory Results are the reader's sentences quoted verbatim, each held by a claim; the Discussion and
  Abstract add no number the reading does not carry; C1's sensitivity pattern and dose-response sign are reported
  as hypotheses for a later pre-registration, not findings.
- Table labels never begin with a numeral ("post window 28 days"): the exhibit check treats a numeral-leading cell
  as a value needing a claim.
- Abstract follows PAPER_PLAN (unstructured, 250 words; names NYC EMS Incident Dispatch Data, New York City,
  2015–2024, the linkage, the design, the three-way question).
- CP3 fixes prepared while the refuters ran and applied only after the workflow returned, so the refuters judged
  the text the finders saw; items the refuters killed on materiality were still applied where a sentence made the
  paper more exact (family-corrected bound, "nor the opposite", MDE-vs-test, Table 5 caption, external deposit).
- When the paper paraphrases the record it uses the record's own nouns; the five CP3 survivors (P59–P63) are held
  by suite tokens on the old wording.
- Generated claims are replaced wholesale when the generator changes; every "per stratum" table loops over every
  stratum in the sealed file.
- Figure 3 is drawn from the joint specification the Results report and prints no p-value; Figure 2's axis is in
  SD units of the index.
- The vault carries the memory graph as generated notes under `Graph/`, result values redacted by the renderer.

Related: [[Status]] · [[Next actions]] · [[Graph memory]] · [[00 Project]]
