# Core data manipulation
pandas>=2.0.0
numpy>=1.24.0

# Fast data processing
duckdb>=0.9.0

# Statistical analysis
# pyfixest is the estimation engine for scripts 03-07 and is pinned deliberately:
# 03_main_model.py and 03b_bridge_legacy.py read private attributes (_vcov,
# _coefnames, _r2) that are not API-stable, so an unpinned upgrade can change or
# silently break the joint tests. Verified working: 0.60.0.
pyfixest==0.60.0
statsmodels>=0.14.0
scipy>=1.11.0

# Visualization
matplotlib>=3.7.0

# Data storage
pyarrow>=12.0.0
openpyxl>=3.1.0  # reading the Census/DCP Excel inputs

# Awareness-component fetchers (11b, 11c). Google Trends rate-limits datacenter
# IPs aggressively; the committed outputs mean these rarely need re-running.
pytrends>=4.9.0
